import { memo } from "react";
import { Typography } from "@mui/material";
import { PageTitle } from "@/components";


export const Tag = memo(() => {
  return (
    <>
      <PageTitle>Tag</PageTitle>
      <Typography>tag page</Typography>
    </>
  );
});
